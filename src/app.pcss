/* Write your global styles here, in PostCSS syntax */

@tailwind base;

* {
	box-sizing: border-box;
	margin: 0;
	padding: 0;
}

body {
	max-width: 100vw;
	/* background-color: var(--gray-9);
	color: var(--gray-1); */
}

@tailwind components;

@tailwind utilities;
